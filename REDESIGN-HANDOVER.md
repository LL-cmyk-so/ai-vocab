# ai-vocab 改版 · 交接文档（Handover · 定稿）

> **用途**：本文件是本次品牌改版的**设计规格（定稿）**；落地已完成，因此它同时承担"设计要求"与"实际落地差异"两重身份——**冲突处以下方 §9 的裁决为准**。
> **状态**：✅ **已落地**（品牌改版 `c612b20`，2026-09-06；收尾四批 2026-09-10）。逐项核对、执行记录与验证证据见 `ai-vocab/LANDING-AUDIT.md`。
> **铁律（2026-09-10 修订）**：只动**视觉/布局层**；原「**绝不碰** `script.js`」放宽为**允许为视觉一致性做最小改动**——**搜索 / 折叠 / 路由 / 数据逻辑一律不动**，改一处验一处。

---

## 0. 一句话

**奶油浅底 + 深紫强调 + 无衬线 + 现代简约 + 有编辑气质的"零基础 AI 术语词典"**，含「从这 3 个词开始」上手 hero + 6 主题卡 + 词条详情页。品牌 = 蓝紫书本图标 + "AI 词汇本"文字（左上角）。

**可直接参照的可运行 demo**（强烈建议直接看/抄）：
- 首页：`ai-vocab-brand-v2-demo.html`（工作区根）
- 词条页：`ai-vocab-entry-demo.html`（工作区根）

---

## 1. 品牌 token 层（定死，全站一致，可溯源）

> 这是**显式品牌 override**（非 Semi 默认）；未 override 处仍走现有 Semi 规范（`design/12-双层设计基准.md`）。

| 用途 | 值 |
|---|---|
| 背景（奶油底） | `#F6F1E9` |
| 卡片（暖白） | `#FCFBF8` |
| 主色（深紫） | `#5B4CE7`（hover `#4A3BD0`，浅底 `#ECE8FB`） |
| 正文本 | `#2A2722` |
| 次文本 | `rgba(42,39,34,.62)` |
| 第三文本 | `rgba(42,39,34,.38)` |
| 描边 | `rgba(42,39,34,.10)` |
| 正文字体 | `Inter/-apple-system/PingFang SC...`（**无衬线**） |
| 圆角 | 卡片 12px / 胶囊 9999 / 按钮 3px |
| 阴影 | `0 0 1px rgba(42,39,34,.22), 0 8px 24px rgba(42,39,34,.08)` |
| 动效 | `--ease-out:cubic-bezier(0.23,1,0.32,1)`；`:active` 按压反馈（已有，别动） |

**背景装饰层**：奶油底上放**角落柔色有机 blob**（`#EDE4D3`，低对比）+ 少量紫`✳`星刺（`.4` 透明度），`pointer-events:none`。**不要**绿色 ↗ 箭头（已去掉，易误解）、不要 blob/星刺以外的人人皆有的装饰。

---

## 2. 品牌（左上角）

- **logo 图**：`ai-vocab/AI词汇本-icon.png`（独立蓝紫书本图标）
- 排布：`[书本图标 39px] + [AI 词汇本 22px/800，'AI'用紫 #5B4CE7]`，水平、间距 9px、左上角
- **不要** home/返回键（`ai-vocab` 左边的 home 键已去掉）
- 备选：`ai-vocab/AI词汇本-logo.png`（带副标的完整 lockup，仅作备选，头部不用）

---

## 3. 首页结构（真实内容）

- **顶栏**：左上角品牌（图标+文字）｜右侧 搜索 /「新手教程」（紫实心）。
- **上手 hero**（奶油卡 `--card`，含`从这 3 个词开始`）：
  - 01 人工智能 —— 让机器模拟人类思考与决策的技术
  - 02 大语言模型 (LLM) —— 能读懂和生成文字的大型 AI 模型
  - 03 提示词 —— 你给 AI 的指令，决定它怎么回答
- **六大主题纵览**（奶油卡 `--card`，每张：主题名 + 词数 + 可折叠 + 小节 + 词条 capsule + 进阶徽标）：
  > 下表 2026-09-12 由 `web/words.json` 重新生成（**共 118 词**）。旧版此处的手抄数字（107 词、应用层 20、生态 32、漏「训练方法」整节）已作废。
  - 总纲（2）·无小节：人工智能（AI）、图灵测试
  - 地基（12）：学习与方法（7：机器学习、深度学习、神经网络、训练、推理、算法、数据集）；模型内部与资源（5：参数、权重、算力、GPU、内存荒）
  - 模型本体（24）：大模型家族（8：大语言模型（LLM）、生成式 AI、AIGC、推理模型 / 深度思考、小模型（SLM）·进阶、多模态、NLP、CV）；**训练方法（6：预训练、微调、强化学习与 RLHF、模型蒸馏·进阶、合成数据·进阶、模型崩溃·进阶）**；内部机制（10：Transformer、注意力机制、Token、上下文窗口、思维链·进阶、温度·进阶、MoE·进阶、VLM·进阶、随机鹦鹉·进阶、不透明递归·进阶）
  - 交互层（19）：提示与生成（6）；检索与知识（12：嵌入、向量、向量数据库、语义搜索、全文检索·进阶、分块·进阶、RAG、知识库、召回·进阶、重排序·进阶、联网搜索、AI 搜索）；提示技巧（1）
  - 应用层（25）：Agent 智能体（14：含 Harness/Loop/工具调用/记忆/多智能体/工作流/ReAct/规划/反思/Skill/电脑操控/一人公司/编程智能体）；内容生成（4：含扩散模型）；语音与形象（3）；助手形态（3）；AI 应用开发（1）
  - 生态与前沿（36）：API 工程（7）；评测与安全（16）；内容生态现象（4）；未来方向（6：AGI、ASI、具身智能、世界模型、AI 普惠、智能经济）；数据与飞轮（3：数据飞轮、数据要素、数据治理）
- **页脚**：AI 词汇本 · 给好奇者的零基础 AI 术语词典 · 持续收录中
- **主题不设色块**（去掉每个主题前的彩条）——靠**名字 + 字号层级**区分；**紫色**只在「小节标签 / 进阶徽标 / hover」出现。

---

## 4. 字号层级（从大到小，一条阶梯）

| 元素 | 字号/字重 |
|---|---|
| 品牌 `AI 词汇本` | 22 / 800 |
| hero 标题 `从这3个词开始` | 20 / 800 |
| `6 大主题纵览` | 16 / 700 |
| 主题名 / hero 卡名 | 15 / 700·600 |
| 词条 capsule | 14 |
| **主题下小节标签**（学习与方法…） | **13 / 600 · 紫 #5B4CE7** |
| 词数 / meta / 页脚 | 12 |

---

## 5. 词条详情页（点一个词后）

- **顶栏**：左上角品牌 + 右侧搜索（**无 back 键**）。
- **词条卡**：
  - 标题：词名（28px/800）右侧英文（16px 次要）
  - meta：英文 / 中文 / 别名（12.5px）
  - 分类 tag（模型本体·紫 pill）+ 更新日期（12px）
  - **是什么**：紫色小节标签 + 15px 正文（1.7 行高）
  - **💡 通俗类比**：暖面卡，底色 = **`#F6F1E9`（与首页「人工智能」start-here 卡同色）**，左紫 border + 紫描边，紫标签 + 14px 类比
  - **相关词**：紫标签 + 紫 hover capsule（Token / Transformer / 上下文窗口 / 生成式 AI / 微调 / 提示词）
  - **更多小提示**：`details` 折叠 + 紫 summary（▸ 💡）
  - **上一词 / 下一词**：底部两块导航（紫标题）
- **页脚**小字。

---

## 6. 落地范围与基准（✅ 已完成）

- **实际改动文件**：`web/style.css`、`web/index.html`、`web/script.js`（后者**仅两处**：清掉死色条代码、词条页上/下词导航渲染；搜索 / 折叠 / 路由 / 数据逻辑未动）。
- 原有 `style.css` 已按 Semi 一轮；本次把 `#0064FA`→紫 `#5B4CE7`、白底→`#F6F1E9`、正文→暖黑 `#2A2722`，加 hero + 主题不设色块 + 紫色小节标签 + 品牌。未 override 的间距/字号/动效仍走现有 Semi 规范；本次另把残留冷灰 hover/遮罩与**越档圆角（10 / 8 / 4px）收敛回 token（12 / 9999 / 3）**。
- ⏳ **品牌 override 的契约文件仍未建**：2026-09-10 核查 `design/19-项目设计声明.md` 是**模板 + 判定链**，而 ai-vocab 仓内**尚无 `DESIGN.md` / `PRODUCT.md`**——§1 的品牌 token 目前只活在 `style.css` 的注释里，`16-anti-slop` 的 "outside DESIGN.md" 判定**无据可依**。→ 待补 `ai-vocab/DESIGN.md`（8 小节，品牌层注明「非 Semi 默认、但成体系、可溯源」）。
- **移动端**：沿用现有 `@media(max-width:480px)` + safe-area；hero 三步窄屏折一列。移动端词条标题实取 **22px**（非 28px，避免窄屏断行过多）。

---

## 7. 开放决策（✅ 2026-09-10 全部裁决）

1. **主色蓝→紫是否全站**：✅ 已**全站统一**（全仓 grep `#0064FA` 0 命中）。
2. **阅读深/浅**：✅ 定**浅色**（奶油底），未做暗色变体。
3. **"构成主义主体"**：✅ 按 demo 的**克制版**落地（暖白卡 + 紫标签 + 背景 blob/星刺），不搞重装饰。
4. 搜索/折叠/移动树**功能不动**：✅ 已遵守（`script.js` 仅动死代码与词条页导航渲染）。

---

## 8. 参考指针（需要再读，别复制正文）

- 可运行 demo：`ai-vocab-brand-v2-demo.html`、`ai-vocab-entry-demo.html`（工作区根）
- 品牌图标：`ai-vocab/AI词汇本-icon.png`（头部用）、`ai-vocab/AI词汇本-logo.png`（备选）
- mockup 参考：`AI词汇本浅色现代简约首页设计图...png`
- 现有源码：`ai-vocab/web/style.css`、`index.html`
- 设计引擎：`design/README.md`、`design/12`（基准）、`design/19`（DESIGN.md **模板 + 判定链**；ai-vocab 尚未建 `DESIGN.md`）、`design/16`（anti-slop）

---

## 9. 落地后变更与裁决（2026-09-10 收尾｜逐项证据在 `ai-vocab/LANDING-AUDIT.md`）

本文档作为**设计要求**保留；落地时的实际差异与裁决如下：

| # | 本文档原文 | 实际落地 / 裁决 |
|---|---|---|
| 1 | §3「紫色只在 小节标签 / **进阶徽标** / hover」 | **进阶徽标改浅灰**（与「N 词」计数同色，不抢词条本体）；紫色=小节标签 / hover / 品牌元素 |
| 2 | §5「更多小提示」`details` **折叠** | **默认展开**（`open`），避免有效内容藏在折叠里 |
| 3 | 全站 emoji | **UI chrome 去 emoji**（顶栏 / 页脚 / 子页标题 / 弹窗，共 11 处）；**词条正文分类标签 `📖💡📍⚠️🔀🎯` 保留**（§5 的正文语言） |
| 4 | §3 未提 | **新增背景装饰层**：角落 blob `#EDE4D3` + 紫 `✳` 星刺（§1 已定方向）；demo 的 `.dash/.dot` 按 §1「不要人人皆有的装饰」**未采用** |
| 5 | §5 未提 | **新增词条页底部「上一词 / 下一词」**（按 `words.json` 顺序；首词无上一词、末词无下一词，缺的一侧不占位） |
| 6 | §1 未定 | 圆角统一 12 / 9999 / 3；demo 里 `.np` 的 10px 按"归一档位"改为 12px |
| 7 | §4 字号阶梯 | 全部落地；移动端词条标题取 22px（非 28px） |
| 8 | §3 词数 | 以 `words.json` 为准：**118 词**（旧版 107 / 应用层 20 / 生态 32 作废，见 §3 新表） |

**验证方式**：`node --check` + headless Chrome（DOM 断言 + 1280×900/1100 截图目视）。**不是**"看着对"。

> **仍未做**：① 建 `ai-vocab/DESIGN.md`（见 §6，本仓外 `design/19` 只是模板）；② 本次三批收尾改动尚未 `git commit`（推送需用户明确同意）。
