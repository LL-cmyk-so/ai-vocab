# AI 词汇本 · 迭代路线图（ROADMAP）

> 目标：从"名词字典"进化为"概念 + 关联 + 场景 + 踩坑"的 AI 知识图谱。
> 原则：保持轻量静态站点定位，不做代码教程、不做登录评论、不堆公式。
> 读者定位：待定（日常工具用户 or 开发者生态，见 Phase 3）。

---

## Phase 0：现状（已完成 ✅）

- 56 词白话词典 + 18 步主线 + 精选跳转 + 热门词 + 三视图
- 已上线 Cloudflare Pages（ai-vocab-16n.pages.dev）
- 移动端适配，本地 git 仓库已建好（20 文件，含自动部署 workflow）

---

## Phase 1：词条格式升级（对齐示范模板）★ 上 GitHub 的最小门槛

**解决什么问题**：现在每个词只有一段白话解释，缺类比、缺坑点、缺场景、缺更新标记——这是评估里"内容深度有限 + 更新状态未知"两大短板的核心。

**做什么**：

1. **数据模型升级**（build_words.py + words.json）：
   - 每条词新增字段：`analogy`（通俗类比）、`extra`（进阶区，按需组合：现实场景 / 常见误区 / 容易混淆 / 边界提醒 / 缓解手段 / 适用场景）、`updated`（最后更新日期，脚本自动打）
2. **页面模板升级**：
   - 词条展示：入门释义 → 通俗类比 → `<details>` 可折叠"进阶解读 & 踩坑误区"区 → 相关概念跳转（已有）
   - 词条底部显示"最后更新：xxxx-xx-xx"
   - **正文术语内联链接**（正文里出现的"RAG""Embedding"等直接可点跳转，回应"阅读流被打断"）
3. **内容升级（分 3 批写）**：全部 56 词补齐类比；高频 15 词（幻觉、RAG、Agent、Token、上下文窗口、微调、提示工程、向量、多模态、AGI 等）补齐进阶区（误区 + 场景 + 边界）

**验收标准**：任意词条点开有类比 + 折叠进阶区；高频词有完整坑点；页面有更新日期。

**🚩 上 GitHub 判断点**：Phase 1 完成即可开源。理由：内容已结构化、有更新标记、有进阶区，不再是"裸词典"；开源后社区 PR 可以帮做 Phase 3 的内容深化。

---

## Phase 2：站点功能增强

**解决什么问题**：回应评估里的"离线/导出能力缺失、关联不够、无趣味性"。

**做什么**：

1. **对比专题页**（新增板块，表格形式 + 选型提示）：
   - 专题 1：RAG vs 长上下文（示范已给出）
   - 专题 2：Agent vs 普通提示词
   - 专题 3：微调 vs 提示工程
   - 专题 4：向量检索 vs 关键词检索（FTS5）
2. **一键导出全部词条 Markdown**（生成一个 download 按钮，前端拼装）
3. **小测验**：每个词条 1~2 道判断题（静态数据 + 点击显示答案）
4. **全局更新日志页**：记录每次内容更新的日期与条目

**验收标准**：4 个专题页可访问；导出按钮可用；测验可作答。

**🚩 推荐开源点**：Phase 2 完成 = 功能完整版（有专题、导出、测验），此时开源对外形象最好。

---

## Phase 3：内容深耕（工作量大，分批推进）

**解决什么问题**：从"定义层"走向"现实落地"，覆盖开发者/进阶读者。

**做什么**：

1. **现实场景映射**：每个高频词加"你在 xx 工具/项目里会碰到它"——**映射对象需先定**：
   - 选项 A：日常工具（ChatGPT、豆包、Kimi、Cursor、飞书 AI 里在哪碰到）
   - 选项 B：开发者生态（DSH、Ollama、dsh-docintel 这类）
   - 选项 C：两者都做（日常工具全覆盖 + 开发工具标注为"进阶"）
2. **扩展术语库 +20**：部署类（Workers、限流、速率限制）、Agent 工程类（ReAct、规划模块、反思、记忆模块）、评测类（MMLU、基准测试、BLEU）
3. **过时概念标注**：如"该概念 2023-2024 流行，现在被 xx 替代"
4. **实操小专题**：按任务聚合（"搭本地 Agent 的最小术语集""做 RAG 会遇到的词"）

**验收标准**：词条数 70+；高频词全部有场景映射；至少 2 个实操小专题。

---

## Phase 4：形态升级（可选，按需做）

**解决什么问题**：应对"国内访问不稳定 + 站点下线风险"。

**做什么**：

1. **PWA 离线安装**：浏览器可"添加到主屏幕"，断网也能查（静态站做 PWA 成本低）
2. **开源社区运营**：README 完善、贡献指南（CONTRIBUTING.md）、Issue 模板——开源后逐步做
3. **数据备份机制**：每次发布前自动备份 words.json 到仓库（git 本身就是）

**验收标准**：手机可安装为 App 离线使用；仓库有贡献指南。

---

## 整体节奏建议

```
Phase 1（2~3 周，内容分批）  →  开源（判断点 1）
Phase 2（1~2 周）            →  功能完整（判断点 2，推荐对外宣传点）
Phase 3（持续，社区可参与）  →  知识图谱成型
Phase 4（按需）
```

## 待你拍板的 2 件事

1. **开源时机**：Phase 1 完成就开源（早开源、社区早参与）还是 Phase 2 完成（对外形象完整）？
2. **读者定位**（影响 Phase 3 场景映射方向）：词汇本主要服务日常用户、开发者，还是两者？
