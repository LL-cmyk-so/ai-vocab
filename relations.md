# 关系标注：精选跳转 + 主线路径

> 原则：每个词只连 2~5 个"讲它时必提"的词，不织全量网；另有一条 18 步主线，供零基础读者快速了解 AI 整体。

---

## 第一部分：精选跳转（56 词）

> 格式：词 → 跳转词（为什么跳）

### 第 0 词

**人工智能（AI）** → 机器学习（AI 的核心方法）· 深度学习（最王牌流派）· 大语言模型（当下主角）· AGI（终极目标）

**图灵测试** → 人工智能（它提出的领域）· AGI（衡量智能的现代话题）

### 第一层：地基

**机器学习** → 人工智能（所属领域）· 深度学习（王牌流派）· 训练（学习过程）· 数据集（教材）

**深度学习** → 机器学习（所属流派）· 神经网络（结构基础）· 参数（"深"的来源）

**神经网络** → 深度学习（用它的流派）· 参数（内部旋钮）· 权重（关键参数）

**训练** → 推理（成对概念）· 数据集（教材）· 预训练（大模型版训练）

**推理** → 训练（成对概念）· API（按次计费的场景）

**参数** → 权重（最核心的参数）· 训练（调参数的过程）· 大语言模型（参数大户）

**权重** → 参数（所属）· 神经网络（存在于其中的连接）

**数据集** → 训练（用途）· 预训练（海量数据）· 微调（少量精选数据）

**算法** → 机器学习（AI 算法集中地）· 算力（执行算法的马力）

**算力** → GPU（算力来源）· 训练（最耗算力的环节）· 本地部署（个人算力瓶颈）

**GPU** → 算力（它提供的）· 训练（主要用途）· 本地部署（硬件门槛）

### 第二层：模型本体

**大语言模型（LLM）** → Token（计量单位）· Transformer（底层架构）· 上下文窗口（能力边界）· 生成式 AI（所属大类）

**生成式 AI** → 大语言模型（最出名的代表）· 文生图 / 文生视频（生成应用）· 多模态（新形态）

**自然语言处理（NLP）** → 大语言模型（巅峰形态）· Transformer（技术突破）

**Transformer** → 注意力机制（核心组件）· 大语言模型（建立在它之上）

**注意力机制** → Transformer（所在的架构）

**Token（词元）** → 大语言模型（计量对象）· 上下文窗口（按 token 计）· API（按 token 计费）

**上下文窗口** → Token（计量单位）· 记忆（人/机短期记忆对应）· 大语言模型（能力边界）

**预训练** → 微调（成对概念）· 训练（所属）· 大语言模型（预训练的对象）

**微调** → 预训练（成对概念）· 数据集（用少量精选数据）· 对齐（进一步调教）

**强化学习与 RLHF** → 训练（所属大类）· 对齐（RLHF 是对齐的手段）· 微调（同属训练阶段）

**多模态** → 生成式 AI（所属）· 文生图 / 文生视频（多模态生成）· 数字人（多模态应用）

### 第三层：交互层

**提示词（Prompt）** → 提示工程（方法论）· 大语言模型（交互对象）· 越狱 / 提示词注入（恶意利用）

**提示工程** → 提示词（研究对象）· Agent（进阶用法）

**嵌入（Embedding）** → 向量（产物）· 语义搜索（用途）· RAG（关键步骤）

**向量** → 嵌入（产生方式）· 向量数据库（存放处）· 语义搜索（用途）

**向量数据库** → 向量（存的东西）· RAG（主要应用）· 记忆（AI 长期记忆载体）

**语义搜索** → 向量（计算基础）· 向量数据库（实现工具）

**检索增强生成（RAG）** → 知识库（资料源）· 向量数据库（检索工具）· 幻觉（要解决的问题）· 嵌入（把资料向量化）

**知识库** → RAG（配合使用）· 向量数据库（存储方式）

**幻觉** → RAG（缓解手段）· 对齐（治理手段）· 提示词（与提问方式相关）

### 第四层：应用层

**聊天机器人 / 对话式 AI** → 大语言模型（驱动）· AI 助手（升级形态）· NLP（出身领域）

**AI 助手** → 聊天机器人（基础形态）· Agent（进化方向）· 工具调用（能力来源）

**Copilot（编程副驾驶）** → AI 助手（同类产品）· 大语言模型（驱动）

**文生图** → 生成式 AI（所属）· 文生视频（升级版）

**文生视频** → 文生图（基础版）· 世界模型（理解物理后的进阶）

**语音识别** → 语音合成（成对概念）· 数字人（组合应用）

**语音合成** → 语音识别（成对概念）· 数字人（组合应用）

**数字人** → 语音合成（让它说话）· 语音识别（让它听懂）· 大语言模型（它的"大脑"）

**Agent（智能体）** → 工具调用（能干活的关键）· 记忆（长期工作前提）· 多智能体（进阶形态）· AI 助手（前身）

**工作流** → Agent（执行者）· 工具调用（各步骤能力）· 提示工程（编排思路）

**工具调用** → Agent（使用者）· API（调用对象）

**记忆** → 向量数据库（长期记忆载体）· 上下文窗口（短期记忆）· Agent（需要记忆的干活者）

**多智能体** → Agent（单个组成单元）· 工作流（协作编排）

### 第五层：生态与前沿

**API** → 工具调用（AI 调用 API 干活）· Token（计费单位）· 大语言模型（API 背后）

**开源模型 / 闭源模型** → 本地部署（开源才能本地跑）· API（闭源的获取方式）· 模型评测（对比两者）

**本地部署** → 开源模型（前提）· GPU（硬件要求）· 算力（瓶颈）

**模型评测** → 大语言模型（评测对象）· 开源 / 闭源（常被拿来对比评测）

**对齐（Alignment）** → 强化学习与 RLHF（实现手段）· 幻觉（要治理的问题）· AGI（为什么重要）

**可解释性** → 对齐（相关安全课题）· AGI（终极相关）

**AGI（通用人工智能）** → 人工智能（所属）· 世界模型（通往之路）· 对齐（安全问题）· 具身智能（形态之一）

**具身智能** → 世界模型（关键能力）· AGI（目标）· 多模态（感知基础）

**世界模型** → AGI（通往之路）· 具身智能（应用）· 文生视频（直观体现）

**越狱 / 提示词注入** → 提示词（攻击入口）· 对齐（防御手段）

---

## 第二部分：主线路径（18 步，零基础从这开始）

> 顺着走一遍，AI 是什么、怎么来的、能干什么、要去哪，就全清楚了。每步标注了"这一步解决什么问题"。

| 步 | 词 | 这一步解决什么问题 |
|---|---|---|
| 1 | 人工智能（AI） | 总纲：AI 到底是什么 |
| 2 | 机器学习 | AI 的核心方法：让机器自己学会 |
| 3 | 深度学习 | 机器学习里最厉害的流派 |
| 4 | 神经网络 | 深度学习的"大脑"长什么样 |
| 5 | 训练 | 怎么"教"这个大脑（上学） |
| 6 | 参数 | 训练到底在调什么（旋钮） |
| 7 | 推理 | 学完之后怎么用（考试） |
| 8 | 算力 / GPU | 训练为什么这么烧钱（马力） |
| 9 | 大语言模型（LLM） | 集大成者：AI 时代的主角登场 |
| 10 | Token | 大模型怎么"读"文字（积木块） |
| 11 | 上下文窗口 | 大模型一次能记住多少 |
| 12 | 预训练 / 微调 | 大模型是怎么造出来的 |
| 13 | 提示词（Prompt） | 你和大模型对话的入口 |
| 14 | 幻觉 | 用之前必须知道的坑 |
| 15 | 聊天机器人 / AI 助手 | 最常见的 AI 产品形态 |
| 16 | Agent（智能体） | AI 从"说"到"做"的进化 |
| 17 | RAG | 让 AI 用上你的私有资料 |
| 18 | AGI | 终点站：AI 要去哪 |

**主线逻辑一句话**：AI 是什么（1）→ 怎么工作的（2~8）→ 主角模型怎么来的（9~12）→ 普通人怎么用（13~15）→ 正在发生的进化（16~17）→ 未来（18）。
